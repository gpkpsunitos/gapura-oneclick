/**
 * @file
 * Dibuat oleh Claude
 * 
 * File ini berisi halaman dashboard Divisi HC (Human Capital),
 * menampilkan workspace dan analitik untuk divisi HC.
 */

import { HCWorkspaceDashboard } from '@/components/hc/HCWorkspaceDashboard';

/**
 * Komponen halaman dashboard Divisi HC (Human Capital)
 * Menampilkan workspace dan analitik untuk divisi HC
 * @returns {JSX.Element} Tampilan dashboard Divisi HC
 */
export default function HCDashboardPage() {
    return <HCWorkspaceDashboard />;
}
